# Homis自動カルテ生成

## 使い方

### 1. 初回セットアップ
`install.bat` をダブルクリック（またはPowerShellで `.\install.bat`）

### 2. 起動
`start.bat` をダブルクリック

---

## 設定（config.json）

| 設定 | 説明 |
|---|---|
| `watch_folder` | JSONファイル監視フォルダ |
| `test_mode` | true=テスト, false=本番 |
| `headless` | true=バックグラウンド, false=ブラウザ表示 |
